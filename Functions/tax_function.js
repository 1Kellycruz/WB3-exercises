function getSocSecTax(grossPay, socialSecuirty, taxRate) { 
    const G
    let tempC = (tempF - 32) * (5 / 9);
    return tempC;
}
let tempF = 0;
let tempC = convertFtoC(tempF);
console.log(`Current Temp: ${tempF} F, convert to ${Math.round(tempC)} C`);